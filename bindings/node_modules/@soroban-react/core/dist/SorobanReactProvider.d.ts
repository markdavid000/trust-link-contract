import { Connector, ContractDeploymentInfo, WalletChain } from '@soroban-react/types';
import React from 'react';
import * as StellarSdk from '@stellar/stellar-sdk';
/**
 * Props for the SorobanReactProvider component.
 */
export interface SorobanReactProviderProps {
    appName?: string;
    autoconnect?: boolean;
    chains: WalletChain[];
    activeChain?: WalletChain;
    children: React.ReactNode;
    connectors: Connector[];
    server?: StellarSdk.SorobanRpc.Server;
    serverHorizon?: StellarSdk.Horizon.Server;
    deployments?: ContractDeploymentInfo[];
}
/**
 * Converts a Soroban RPC URL to a Soroban RPC server object.
 * @param {string} sorobanRpcUrl - Soroban RPC URL.
 * @returns {StellarSdk.SorobanRpc.Server} - Soroban RPC server object.
 */
export declare function fromURLToServer(sorobanRpcUrl: string): StellarSdk.SorobanRpc.Server;
/**
 * Converts a horizon network URL to a Horizon server object.
 * @param {string} networkUrl - Network URL.
 * @returns {StellarSdk.Horizon.Server} - Horizon server object.
 */
export declare function fromURLToHorizonServer(networkUrl: string): StellarSdk.Horizon.Server;
/**
 * SorobanReactProvider component.
 * Provides context for Soroban React application.
 * @param {SorobanReactProviderProps} props - Props for the component.
 */
export declare function SorobanReactProvider({ appName, autoconnect, chains, activeChain, // Non mandatory fields default to default Context fields value
children, connectors, deployments, server, serverHorizon, }: SorobanReactProviderProps): React.JSX.Element;
